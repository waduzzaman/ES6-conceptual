const number = [1, 2, 4, 5, 6, 7, 8, 12];

// get the number which is greater than 5. find will give the only first number close after five from other numbers 

const result = number.find(item => item > 5);
console.log(result);
