const add = (x=5, y=2) => {
    return x + y;
}

// console.log(add(7));

//if we give the argument then the argument value will be replace otherwise the default parameter value will work 
console.log(add(2, 2));